services:
  redis:
    image: redis:7-alpine
    container_name: arbitrum-redis
    ports:
      - "6379:6379"
    command: ["redis-server", "--appendonly", "yes"]
    volumes:
      - arbitrum-redis-data:/data

volumes:
  arbitrum-redis-data:
